{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
    "compilerOptions": {
    "strict": true,               // Enables all strict type-checking options
      "noUnusedLocals": false,        // Disable errors for unused variables
      "noUnusedParameters": false,    // Disable errors for unused parameters
      "noImplicitAny": true,          // Ensure explicit typing for variables and parameters
      "strictNullChecks": true,       // Ensure proper handling of null and undefined
      "strictFunctionTypes": true     // Ensure strict function type checks
    }
}
